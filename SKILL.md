---
name: travel-planner
description: 旅行出行规划：输入日期+目的地+偏好，调用高德地图 MCP 获取天气/景点/美食/路线数据，生成清新手账风的单页 HTML 出行手账（交互地图、逐日行程时间轴、美食推荐、景点间交通、预算、住宿建议、出行贴士）。用户提到旅行规划、出行计划、旅游攻略、行程安排、自由行、周边游、去某地玩几天、带家人出游、出差顺带游玩等任何出行规划场景时使用，即使没明说"生成页面"也用它。
---

# 旅行出行规划（高德地图 × 手账风页面）

输入「日期 + 目的地 + 偏好」，产出一份清新手账风的**单文件 HTML 出行手账**：
天气、逐日行程时间轴（含景点间交通衔接）、交互式高德地图（贴纸 marker + 当日动线虚线）、美食推荐、预算参考、住宿区域建议、出行贴士。

## 技能文件

- `references/setup-amap.md` — 高德 Key 申请与 MCP 配置步骤（**前置依赖不满足时必读**）
- `references/amap-mcp.md` — 高德 MCP 工具清单与取数策略（取数阶段必读）
- `references/page-design.md` — 页面模板结构、PLAN 数据 schema、行程编排与文案标准（生成页面阶段必读）
- `assets/template.html` — 页面模板本体（复制后填充数据，禁止从零重写页面）

## 工作流程

### 0. 解析输入
从用户的话里提取：目的地、出发/返回日期、天数、人数、偏好（美食/人文/自然/亲子/节奏快慢）、交通偏好。
- 相对日期（"下周末""十一假期"）按**今天的真实日期**换算成具体日期。
- 缺目的地或日期：先问清再继续。天数/人数/偏好可给默认值（3 天、2 人、均衡慢节奏）并在交付时说明。

### 1. 前置检查（两把钥匙，缺一只降级对应能力，不阻塞非地图部分）
- **MCP 可用性**：会话里找不到 `maps_` 开头的高德 MCP 工具 → 读 `references/setup-amap.md`，给用户配置指引。用户明确说"先看预览"时，可用模型知识先出一版，但必须在页面顶部加「⚠️ 预览版：数据未经高德校验，仅供风格确认」横幅，并在交付时说明。
- **页面地图 Key**：读 `~/.travel-planner.json`（结构 `{"amapJsKey":"...","amapSecurityJsCode":"..."}`）。不存在 → 向用户要一次 Web端(JS API) Key 和安全密钥并写盘保存；**永远不要在对话里回显完整 key**。

### 2. 取数（先读 `references/amap-mcp.md`）
地理编码定位城市 → 天气查询 → 景点 POI 关键词搜索 → 美食 POI 搜索 → 相邻景点间路径规划（步行/公交）。
天气超出高德预报窗口（出发日距今 >3 天）时走 climate 模式，页面会自动显示"气候参考"说明。

### 3. 行程编排（标准见 `references/page-design.md`）
按商圈/地理聚类分天（一天一个区域，减少折返）；每天 4~6 个节点含 2 顿正餐；
相邻节点步行 ≤25 分钟优先步行，否则地铁/打车；时间安排符合常理（不早于 8:00 出发、不晚于 22:30 结束、午餐 11:30–13:00、晚餐 17:30–19:00、单景点停留标注合理时长）。

### 4. 生成页面（先读 `references/page-design.md`）
1. 复制 `assets/template.html` 到当前工作目录 `travel-plans/<目的地>-<YYYYMMDD>.html`（起始日期）。
2. 只改两处：`<title>` 与 `<script type="application/json" id="plan-data">` 里的 JSON。模板的 CSS/JS 渲染器不要动。
3. 把 `~/.travel-planner.json` 里的两个值填进 `PLAN.amap`。

### 5. 校验
1. PLAN 合法性（模板要求数据必须是合法 JSON，无注释无尾逗号）：
   ```bash
   python -c "import json,re,sys; h=open(sys.argv[1],encoding='utf-8').read(); m=re.search(r'<script type=\"application/json\" id=\"plan-data\">(.*?)</script>',h,re.S); json.loads(m.group(1)); print('PLAN JSON OK')" travel-plans/目标文件.html
   ```
2. 会话里有浏览器控制技能（browser-use 等）时：打开该文件截图检查——console 无报错、地图 marker/动线正常显示、手账风样式正确；无浏览器技能则把路径交给用户并说明直接双击打开。

### 6. 交付
- 给出页面文件的绝对路径（markdown 链接）。
- 附一段摘要：每天主题与亮点、总预算/人、住宿区域、特别提醒。
- 提醒：出发前 3 天内可重新生成一次，刷新为高德实时天气预报。

## 硬性要求

- 产出**单个自包含 HTML**，除高德地图 CDN 与可选景点图片外无任何外部依赖，离线打开时除地图外所有模块正常。
- 全中文文案，口吻轻松有手账感（"这一站""记得""别错过"），但信息必须具体真实（价格、耗时、班次来自 MCP 数据或明确标注为估算）。
- `PLAN` 数据必须是合法 JSON；预算表总和由页面自动计算，items 里的 amount 填单人数值。
- 没有坐标的节点（如"返程出发"）`lng/lat` 留空即可，地图自动跳过、时间轴正常显示。
